import rootColor from './rootColor';

const globalStackOptions = {
  tabBarStyle: {
    backgroundColor: rootColor.containerColor,
    // paddingBottom: 4,
  },
  headerShown: false,
  tabBarInactiveTintColor: rootColor.smokeColor,
  tabBarActiveTintColor: rootColor.whiteColor,
};

export default globalStackOptions;
